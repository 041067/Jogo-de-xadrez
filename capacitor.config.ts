import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.senai.jogoxadrez',
  appName: ' Jogo de Xadrez',
  webDir: 'out',
};

export default config;
